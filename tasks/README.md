# tasks/

This folder contains **TaskSpec** JSON files.

Workflow:
1) Write a TaskSpec JSON (v0.1).
2) Run `codex exec ...` to implement it.
3) Run tests + validate the TaskResult JSON against the schema.
4) Commit changes.
